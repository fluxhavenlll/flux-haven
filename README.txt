FLUX HAVEN — GITHUB READY FILES

Use these three files together:
- index.html
- app.js
- styles.css

The Supabase Daily Surprise Gift + Live Presence SQL has already been run in the database.

IMPORTANT:
The app.js in this package is aligned with the SQL version that returns:
- admin_get_today_gift_code: id, code, generated_for, reward_min, reward_max, claim_limit, claim_count, created_at
- admin_generate_today_gift_code: id, code, generated_for, reward_min, reward_max, claim_limit, created_at
- admin_get_gift_claims: username, phone, reward, claimed_at

Do not rename the files or change the relative paths unless you also update index.html.
